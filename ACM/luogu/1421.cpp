#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[])
{
    const int x = 19;
    int a, b;
    while (cin >> a >> b)
        cout << (a * 10 + b) / x << endl;
    return 0;
}