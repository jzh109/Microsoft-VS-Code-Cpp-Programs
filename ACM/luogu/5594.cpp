#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[])
{
    cin.tie(NULL);
    cout.tie(NULL);
    ios::sync_with_stdio(false);
    int n, m, k;
    map<int, int> mm;

    while (cin >> n >> m >> k)
    {
        mm.clear();
        }

    return 0;
}