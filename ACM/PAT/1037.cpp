#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int a, b, c, d, e, f, x, y, z, r = 0;
    char t;
    cin >> a >> t >> b >> t >> c;
    cin >> d >> t >> e >> t >> f;
    if (f - c < 0)
        r = -1;
    z = abs(f - c);
    return 0;
}
